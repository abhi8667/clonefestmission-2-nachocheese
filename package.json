{
  "name": "triarc-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w packages/shared-types && npm run build -w packages/engine && npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run test -w packages/engine && npm run test -w apps/api",
    "test:engine": "npm run test -w packages/engine",
    "seed": "npm run seed -w apps/api",
    "typecheck": "npm run typecheck -w packages/shared-types && npm run typecheck -w packages/engine && npm run typecheck -w apps/api && npm run typecheck -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
